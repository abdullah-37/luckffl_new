import 'package:flutter/material.dart';

class App062 extends StatelessWidget {
  const App062({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('App 062')));
  }
}
